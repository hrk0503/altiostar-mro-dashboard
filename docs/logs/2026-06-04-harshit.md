# Daily Log — Harshit Kumar — June 4, 2026

## Done today
- Phase 2 Day 2 — built and deployed the full automated pipeline + interactive dashboard
- Built `ScenarioLoader` class (~380 lines) — loads scenario YAMLs (`rush_hour`, `rain_fade`, `tower_failure`), applies multipliers to env parameters, with full validation and rollback support. 35 unit tests.
- Wired scenarios into `MROEnv` — env now accepts `scenario` parameter, auto-applies cell modifications at reset. 13 integration tests.
- Built `run_experiment.py` master pipeline (~450 lines) — `--sweep-all` runs 16 experiments (4 variants x 4 scenarios), logs to JSON + MLflow, generates comparison table. Nicolas's automation requirement complete.
- Built interactive Streamlit dashboard (`src/dashboard/app.py`, ~900 lines):
  - Tailwind-inspired design with Inter + JetBrains Mono fonts, dark navy sidebar
  - 6 pages: Dashboard (KPIs + donut + tables), Cell Map (scattermapbox), Experiments (variant cards + radar + heatmap), Network (2D topology), Simulation (live agent), Reports (KPI delta + gate checklist)
  - Password gate (`Winniio-2019`) with URL persistence via `st.query_params`
  - Real-time JS clock showing local + JST timezone
  - WINNIIO branding — crane logo in sidebar, header, favicon, login screen
  - Top feature bar with Anomaly Scan, Health Check, Quick Export panels
- Deployed dashboard to Streamlit Cloud:
  - Created public repo `hrk0503/altiostar-mro-dashboard` for deployment (private fork can't be made public)
  - Set up auto-sync GitHub Action — syncs from LifeAtlas upstream every 15 min
  - Live at https://altiostar-mro-dashboard.streamlit.app
- Pushed 3 commits to staging: `dedc7fd` (ScenarioLoader + pipeline + dashboard v1), `a2e280b` (Tailwind redesign), `abe9182` (plotly + Streamlit config)
- Reviewed Shourya's PPO training results (v0-v3) — identified all variants showing identical HO%=79.27% due to low timesteps (1000). Need 25K+ for differentiation.
- Reviewed Ananyaa's reward variant comparison — confirmed V0 misleading (raw counts), V2 best performer (80.1% HO, reward 73.4). Told her to change `mro_default.yaml` reward_version from v0 to v2.
- Reviewed Ananyaa's sweep results (16/16 passed, 2420s) — same timestep issue confirmed across all scenarios.
- Coordinated team: redirected Devika on task (QA on scenarios), got Shourya to run PPO training, got Ananyaa to run comparison suite + full sweep
- Updated Nicolas with dashboard link and password via WhatsApp
- Sent daily standup messages, tracked all team deliverables

## Blockers
- Training timesteps too low (1000) — all variants produce identical KPIs. Need 25K-50K for meaningful results. Planned for tomorrow.

## Tomorrow
- Run proper training with increased timesteps: `python3 run_experiment.py --sweep-all --timesteps 25000 --eval-episodes 10` (~40 min)
- Change `mro_default.yaml` reward_version v0 to v2
- Merge Ananyaa's sweep results + reward_variant_comparison.json onto staging
- Push real results to dashboard — live update
- Polish dashboard, fix any remaining bugs
- Gate G3 summary — Phase 2 must close (Sat-Sun holiday, Mon = Phase 3)
- Update Nicolas with latest dashboard URL

## Hours worked: 8

## Confidence I'll hit this week's gate: 5
