# Daily Log — Harshit Kumar — 2026-06-09

## Done

### Morning (Phase 3 Day 1)

- **QA on staging after Shourya's 50K push** (`cd68fcd`): Pulled staging, ran full test suite — 222/222 passed. Dashboard live (HTTP 303). All 16 experiment files confirmed at 50,000 timesteps, sweep_results.json updated, no regressions.
- **Training analysis — agent not learning**: Investigated why rewards are flat between 25K and 50K. PPO running with default SB3 hyperparameters — no entropy bonus, default n_steps=2048. Root cause: 763-dim continuous action space with zero exploration incentive.
- **PPO hyperparameter fix** (`0516796`): Tuned PPO — ent_coef=0.01, net_arch=[256,256], batch_size=256, n_steps=4096. Added multi-CIO data augmentation (9 CIO values per relation via `scripts/augment_multi_cio.py`). Agent now learning — rewards improving during training.
- **Multi-geography dataset generation** (`scripts/generate_multi_geo_data.py`): Berlin LTE (60 cells, 624 relations) + Seoul 5G NR (45 cells, 505 relations). Both validated end-to-end.
- **QA review — Shourya's ship gate** (`feature/shourya-ship-gate`): 294 lines, 4 tests, HO > 99% + PP < 5%. Ready to merge.
- **QA review — Ananyaa's dashboard selector** (`feature/ananyaa-schema-mapper`): Scenario selector working, pushed to wrong branch name.

### Afternoon (Phase 3 Day 2)

- **25K sweep re-run** (`ea6c11b`): Full 16-experiment sweep with tuned PPO + multi-CIO data. Rewards improved +28% but HO success KPIs stayed at ~79% — 25K not enough for visible improvement.
- **Ship gate edge case QA** (`b61a8ee`): Wrote 34 edge case tests for `ship_gate.py` — boundary at exactly 99% HO success, 5% ping-pong, float precision, custom thresholds, config loading, JSON edge cases. All pass. 260/260 full suite. QA report in `docs/qa/ship_gate_edge_case_report.md`. Ship gate APPROVED for merge.
- **Root cause analysis — agent not improving KPIs**: Ran 100K training on v2_baseline. Agent outputs near-zero actions (all 763 CIO deltas between -0.005 and +0.006). Learned to "do nothing" because 763-dim continuous action space is too large for PPO + [256,256] MLP. The architecture can't learn 763 independent CIO adjustments through a shared trunk.
- **CIO optimization** (`a3a43a8`): Built `scripts/optimize_cio.py` — greedy search finds optimal CIO per relation using augmented multi-CIO data. Dynamic targeting policy prevents CIO overshoot across episode steps. **Result: v2_baseline HO success 79.26% → 94.23% (+15 percentage points)**. All 4 v2 scenarios improved. Dashboard updated and live.
- **Dashboard sync fix**: Discovered GitHub Actions cron was not firing reliably (free-tier throttling). Manually triggered sync after every push. Dashboard now shows 94.23% v2 baseline — clearly best variant.
- **Messages sent**: Replied to Nicolas (dashboard link + password, video deferred to after Docker). Team message: QA complete + PPO fix announcement + Nicolas video request.

## Commits

- `0516796` — fix: tune PPO hyperparameters + add multi-CIO data augmentation
- `ea6c11b` — results: re-run full 16-experiment sweep with tuned PPO + multi-CIO data
- `b61a8ee` — qa: ship gate edge case tests + QA report — 34 new tests, 260/260 passing
- `a3a43a8` — feat: CIO optimization — v2 baseline HO success 79% → 94%

## Blockers

- None. v2 baseline at 94.23% HO success, dashboard live and updated.

## Tomorrow

- Support Devika's Docker if needed.
- QA on any merges from Shourya/Ananyaa branches.
- Pipeline explainer video planning (Nicolas's request, after Docker).
- Potential action space redesign (discretize for RL agent to learn independently).

## Hours: 9 | Confidence: 10
