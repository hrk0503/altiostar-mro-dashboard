# Daily Log — Harshit Kumar — 2026-06-09

## Done

- **QA on staging after Shourya's 50K push** (`cd68fcd`): Pulled staging, ran full test suite — 222/222 passed. Dashboard live (HTTP 303). All 16 experiment files confirmed at 50,000 timesteps, sweep_results.json updated, no regressions.
- **Training analysis — agent not learning**: Investigated why rewards are flat between 25K and 50K. Findings: PPO is running with all default SB3 hyperparameters — no entropy bonus (`ent_coef=0.0`), no custom learning rate, default `n_steps=2048`. With a 763-dimensional continuous action space (CIO deltas), the agent has zero exploration incentive. Training trajectory shows 0.01% reward change across 17 episodes. Agent HO success (79.27%) equals random baseline (79.25%) — within noise. Root cause is not timesteps, it's hyperparameter config. Recommended: add entropy bonus (0.01–0.05), increase learning rate, increase n_steps, potentially discretize actions.
- **Public handover dataset research**: Searched Kaggle, Zenodo, GitHub, 3GPP sources, academic repos for real LTE/5G handover CSV data. Found Vienna 4G/5G Drive-Test Dataset (Zenodo) and NUWiNS multi-carrier dataset (GitHub). Neither has exact MRO PM counter schema — this is operator-proprietary data.
- **Multi-geography dataset generation** (`scripts/generate_multi_geo_data.py`): Built a reusable generator that creates pipeline-compatible CSVs for any city. Generated two new clusters:
  - **Berlin LTE** (`data/berlin_lte/`): 20 sites, 60 cells, 624 relations, Band 3/7, Nokia, 1.8M PM rows
  - **Seoul 5G NR** (`data/seoul_5g/`): 15 sites, 45 cells, 505 relations, n78/n257, Samsung, 1.5M PM rows
- **End-to-end pipeline validation**: Both datasets passed through MROEnv + PPO training successfully. All 4 CSV schemas match Tokyo format exactly. Results saved to `results/multi_geo_validation.json`. Proves geography-agnostic replicability — same pipeline, different city, different vendor, different technology (LTE vs 5G NR).
- **Tests**: 222/222 still passing after all changes.
- **.gitignore merge conflict** (`951d1cb`): Fixed unresolved conflict markers from Shourya's Saturday merge — added berlin_lte/, seoul_5g/, mlflow.db, .claude/, checkpoints/ to ignores.
- **QA review — Shourya's ship gate** (`feature/shourya-ship-gate`): Reviewed `src/pipeline/ship_gate.py` (294 lines) + `tests/test_ship_gate.py` (4 tests). Thresholds: HO Success > 99%, Ping-Pong < 5%. Handles single experiments + sweep JSONs. Config YAML loading with fraction-to-percent conversion. All 4 tests pass. Full suite with ship gate: 226/226. Clean, ready to merge.
- **QA review — Ananyaa's dashboard selector** (`feature/ananyaa-schema-mapper`): Reviewed scenario selector dropdown (baseline, rush_hour, rain_fade, tower_failure). KPIs update dynamically per scenario. SC_META properly defined. Code is sound. Note: she pushed to her old Phase 2 branch (`feature/ananyaa-schema-mapper`) instead of `feature/ananyaa-dashboard` as assigned.
- **Sent PPO findings** to team WhatsApp — flagged that default hyperparameters + 763-dim action space with zero entropy means agent can't explore.

## Blockers

- Agent not beating random baseline — PPO hyperparameters need tuning before more training is useful. Flagged for team.

## Tomorrow

- Support Devika's Docker setup if needed.
- If Devika addresses PPO tuning, support with hyperparameter recommendations.
- Full training runs on Berlin/Seoul if Nicolas approves.
- QA on any merges from Shourya/Ananyaa branches.

## Hours: 7 | Confidence: 10
